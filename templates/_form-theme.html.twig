{% use 'tailwind_2_layout.html.twig' %}

{% block form_help %}
    {%- if help -%}
        <div class="flex items-start gap-2">
            <span class="mt-1">ℹ️</span>
            {{ parent() }}
        </div>
    {%- endif -%}
{% endblock %}

{% block credits_widget %}
{#    {{ dump() }}#}
    <div class="flex border border-gray-300 overflow-hidden">
        <span class="px-3 flex items-center bg-gray-100 text-gray-500">{{ units_symbol }}</span>
        {{ block('form_widget') }}
    </div>
{% endblock %}

{% block form_label_content %}
    {{ parent() }}
    {% if tooltip is defined and tooltip %}
        <span class="ml-1 cursor-help text-gray-400" title="{{ tooltip }}">ℹ️</span>
    {% endif %}
{% endblock %}
