diff --git a/config/packages/cache.yaml b/config/packages/cache.yaml
index 7050b11..9069c40 100644
--- a/config/packages/cache.yaml
+++ b/config/packages/cache.yaml
@@ -17,5 +17,5 @@ framework:
         app: cache.adapter.array
 
         # Namespaced pools use the above "app" backend by default
-        #pools:
-            #my.dedicated.cache: null
+        pools:
+            iss_location_pool: null
diff --git a/sfcasts/outline.md b/sfcasts/outline.md
index 5366365..bbf79b4 100644
--- a/sfcasts/outline.md
+++ b/sfcasts/outline.md
@@ -152,6 +152,7 @@
 - Update to see it in the dump
 - Remove the dump
 - See `cache.adapter.array` in action
+- TODO We need to revert that `cache.adapter.array` somewhere, as it almost means no cache now 
 
 ## Autowiring
 
diff --git a/src/Controller/MainController.php b/src/Controller/MainController.php
index 9614ad8..f1626cc 100644
--- a/src/Controller/MainController.php
+++ b/src/Controller/MainController.php
@@ -17,12 +17,12 @@ class MainController extends AbstractController
     public function homepage(
         StarshipRepository $starshipRepository,
         HttpClientInterface $client,
-        CacheInterface $cache,
+        CacheInterface $issLocationPool,
     ): Response {
         $ships = $starshipRepository->findAll();
         $myShip = $ships[array_rand($ships)];
 
-        $issData = $cache->get('iss_location_data', function (ItemInterface $item) use ($client): array {
+        $issData = $issLocationPool->get('iss_location_data', function (ItemInterface $item) use ($client): array {
             $item->expiresAfter(60); // Cache for 1 minute
 
             $response = $client->request(Request::METHOD_GET, 'https://api.wheretheiss.at/v1/satellites/25544');
